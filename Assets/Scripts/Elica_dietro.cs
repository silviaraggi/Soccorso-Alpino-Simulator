using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;
using DG.Tweening;

public class Elica_dietro : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    public void Rotate(int flag)
    {
        /*if (flag == 0)
            transform.DORotate(new Vector3(0f, 360f, 0f), 0.4f, RotateMode.LocalAxisAdd).SetLoops(-1, LoopType.Restart);
        else if (flag == 1)
            transform.DORotate(new Vector3(0f, 360f, 0f), 1.2f, RotateMode.LocalAxisAdd).SetLoops(-1, LoopType.Restart);*/
    }
}
